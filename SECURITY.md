# Toki 보안 검토

목표는 하나입니다. **API 키나 OAuth 토큰이 Toki 를 통해 유출될 수 없게 만든다.**
검토 시점: 2026-07-30 / 대상: 이 저장소 전체.

## 1. 자격증명이 실제로 어디 있는지

Toki 가 다루는 두 도구의 자격증명 위치를 먼저 확인했습니다.

| 자격증명 | 실제 위치 | Toki 의 접근 |
|---|---|---|
| Codex OAuth 토큰 | `~/.codex/auth.json` (권한 `-rw-------`) | **없음.** 코드에 이 경로가 등장하지 않음 |
| Codex 설정 | `~/.codex/config.toml` (권한 `-rw-------`) | **없음** |
| Claude Code 토큰 | macOS Keychain 항목 `Claude Code-credentials` | **없음.** Security.framework 미링크 |
| Anthropic API 키 | 환경변수 / `~/.config/anthropic/` 프로필 | **없음.** 환경변수를 읽지 않음 |

즉 Toki 는 자격증명을 **획득할 수단 자체가 없습니다.** 탈취 방지를 "잘 보관한다"로
푸는 대신 "애초에 만지지 않는다"로 풀었습니다.

## 2. Toki 가 읽는 것 — 전부

읽기 전용 접근이며 경로는 모두 코드에 하드코딩되어 있습니다.

| 경로 | 추출하는 필드 | 목적 |
|---|---|---|
| `~/.claude/projects/*/*.jsonl` | `type`, `timestamp`, `requestId`/`uuid`, `message.model`, `message.usage.*` 의 토큰 수치 | 5시간/주간 창 집계 |
| `~/.claude.json` | `oauthAccount.organizationRateLimitTier` 또는 `userRateLimitTier` **단 1개** | 플랜 배지("Max 5x") |
| `~/.codex/sessions/**/*.jsonl` | `payload.rate_limits.*`, `payload.info.total_token_usage.total_tokens` | 공식 사용률 · 토큰 수 |
| `~/.config/toki/config.json` | 정수·불리언 몇 개 | 사용자 설정 (유일한 **쓰기** 대상) |
| `claude -p "/usage" --output-format json` | stdout 의 `result` 문자열에서 `N% used` 와 초기화 시각 | Claude 공식 사용률 |
| `codex app-server` (JSON-RPC `account/rateLimits/read`) | 응답의 `usedPercent`·`windowDurationMins`·`resetsAt` 숫자와 `planType` 문자열 | Codex 실시간 사용률 (요청·토큰 소모 없음) |
| `https://api.github.com/repos/osh0678/toki/releases/latest` | 응답 JSON 의 `tag_name` **단 1개** | 새 버전 확인 (유일한 네트워크 사용, 설정에서 끌 수 있음) |

### `claude` CLI 호출 — 왜 이게 더 안전한가

Claude 공식 수치를 얻는 다른 방법은 Keychain 의 OAuth 토큰으로 비공개 엔드포인트를
직접 부르는 것뿐입니다. 그러면 Toki 가 살아있는 토큰을 들고 네트워크를 열어야 하므로
아래 G1·G2 가 동시에 깨집니다. CLI 에 위임하면 **인증과 통신이 CLI 프로세스 안에서**
일어나고 Toki 는 stdout 의 숫자만 읽으므로 두 보증이 그대로 유지됩니다.

대신 서브프로세스 실행 1건이 생기고, 그 지점은 다음과 같이 좁혀두었습니다
(`ClaudeOfficialUsage.swift`):

- 실행 파일은 **절대경로 허용목록**에서만 찾습니다 — `PATH` 나 환경변수, 설정 파일로
  다른 바이너리를 가리킬 수 없습니다.
- 인자는 컴파일 타임 리터럴(`-p`, `/usage`, `--output-format`, `json`)이라 호출자
  입력이 섞일 여지가 없습니다.
- **셸을 경유하지 않습니다.** 인용·이스케이프 문제가 원천적으로 없습니다.
- stdin 은 `/dev/null`, stdout 은 256KB 로 상한, 30초 후 강제 종료합니다.

### 명시적으로 읽지 않는 것

- 세션 로그의 **프롬프트 본문, 도구 출력, 파일 내용** — provider 는 위 숫자 필드만
  꺼내고 나머지는 파싱 결과에서 버립니다. 바이트 사전 필터(`cache_read_input_tokens`,
  `rate_limits`)로 해당 줄만 골라 JSON 파싱합니다.
- `~/.claude.json` 의 PII — 이 파일에는 `emailAddress`, `accountUuid`,
  `organizationUuid`, `displayName`, `organizationName` 등이 있지만 Toki 는
  **rate-limit tier 문자열 하나만** 읽고 그것도 `"Max 5x"` 같은 라벨로 변환해 씁니다.
  원본 문자열은 UI 에 도달하지 않습니다.
- Keychain, 환경변수, `.netrc`, SSH 키 — 전부 미접근.

## 3. 보증 목록

| # | 보증 | 근거 |
|---|---|---|
| G1 | 네트워크는 **업데이트 확인**과, 사용자가 켰을 때의 **업데이트 내려받기**뿐이고, 끄면 0건 | `URLSession` 은 `UpdateChecker.swift`·`UpdateInstaller.swift` 두 곳으로 한정 — 다른 파일에 나타나면 검사 실패. `NWConnection`·`CFStreamCreate`·`CFSocket`·`NSURLConnection` 미사용, `Network.framework` 미링크, 네트워크 entitlement 없음. 아래 § 업데이트 확인 참조 |
| G2 | Keychain 접근 없음 | `SecItem*`/`SecKeychain*` 미사용, `Security.framework` 미링크, `SecItemCopyMatching` 심볼 미임포트 |
| G3 | 외부 프로세스 실행은 **`ClaudeOfficialUsage.swift`·`CodexOfficialUsage.swift` 두 곳**뿐 | 다른 파일에서 `Process`/`NSTask`/`posix_spawn`/`popen`/`NSAppleScript` 가 나타나면 검사 실패. 셸(`/bin/sh` 등) 경유는 전면 금지. 두 파일 모두 실행 파일을 **절대 경로 고정 목록**에서만 찾고(`PATH`·환경변수·설정 파일 미참조), 인자는 컴파일 타임 상수뿐이며, stdout 상한과 타임아웃이 걸려 있습니다. `codex app-server` 는 명령 실행·파일 쓰기 메서드도 노출하므로 Toki 가 **조회 메서드만** 쓴다는 것을 별도 검사로 강제합니다 — 아래 § Codex 실시간 조회 참조 |
| G4 | 디스크 쓰기는 **설정 파일 하나**와, 사용자가 켰을 때의 **Toki 자기 번들 교체**뿐 | `SettingsWriter.swift`·`UpdateInstaller.swift` 외의 파일에서 쓰기 API 가 나타나면 검사 실패. 검사 패턴은 `write(to:)` 류뿐 아니라 `replaceItemAt`·`moveItem`·`copyItem`·`removeItem`·`createDirectory` 까지 포함합니다 — 번들 교체는 바이트를 쓰지 않고 디렉터리를 옮기므로, 좁은 패턴이면 그냥 통과했을 것입니다. 설정 대상은 하드코딩된 `~/.config/toki/config.json`, 원자적 쓰기, 비밀정보 미포함. 번들 교체 대상은 `Bundle.main.bundleURL` 하나뿐이고 **Ed25519 서명 검증을 통과한 뒤에만** 일어납니다. 그 외 모든 파일 접근은 읽기 전용 |
| G5 | 자격증명 경로 미참조 | 소스·바이너리 문자열 모두에 `auth.json`, `sk-ant-`, `sk-proj-`, `id_rsa` 없음 |
| G6 | 경로 이탈 불가 | 심볼릭 링크 스킵 + 후보 파일이 루트 안에 있는지 정규화 경로로 재확인 |
| G7 | 설정으로 경로 조작 불가 | 설정 스키마에 경로/URL 필드가 없고, 수치는 클램프됨 |
| G8 | hardened runtime, entitlement 없음 | `flags=0x10002(adhoc,runtime)`, entitlement 미부여 |
| G9 | 키보드 감시 없음, 마우스는 발생 사실만 | 아래 § 이벤트 모니터 참조. `CGEventTap`·`IOHIDManager`·전역 키보드 모니터는 검사로 차단 |
| G10 | 클립보드는 쓰기만, URL 은 하드코딩된 것만 | `NSPasteboard`·`NSWorkspace.open` 은 `InstallHelpCard.swift`·`UpdateCard.swift`·`UpdateInstaller.swift` 로 한정. `UpdateInstaller` 의 용도는 교체를 마친 **자기 번들 재실행** 하나뿐입니다. 클립보드 **읽기** API 는 검사로 차단 |

### 이벤트 모니터

패널을 "바깥을 클릭하면 닫히게" 만들려면 앱 밖의 입력을 알아야 합니다. 모니터는 **하나**
뿐이고, **패널이 떠 있는 동안에만** 등록되며 닫으면 해제됩니다. 접근성·입력 모니터링
권한을 요구하지 않습니다.

| 모니터 | 보는 것 | 쓰는 것 |
|---|---|---|
| 전역 마우스 다운 | *다른* 앱으로 전달된 클릭 | **발생 사실만.** 이벤트 값은 버리고 좌표도 읽지 않습니다 — 전역 모니터에 도달한 것 자체가 "Toki 바깥"을 뜻하기 때문입니다 |

키보드 모니터는 전역이든 로컬이든 **하나도 없습니다.** 전역 키보드 모니터는 머신 전체의
모든 키 입력을 관찰하게 되므로 애초에 배제했고, `security-check.sh` 가 이를 차단합니다.
그 대가로 Esc 나 ⌘ 단축키는 동작하지 않습니다 — Dock 아이콘이 없는 accessory 앱의 패널은
키 윈도우가 되지 못해 키 이벤트가 도달하지 않으며, 이를 우회하는 유일한 수단이 바로 전역
키보드 감시입니다. 편의보다 이 보증을 택했습니다.

### 설치 도움 UI

CLI 가 없을 때 설치를 돕지만, **Toki 가 설치 명령을 실행하지는 않습니다.** `brew`/`npm`/
파이프 셸 스크립트를 앱이 돌리면 임의 코드 실행 통로가 열려 G3 이 무의미해집니다. 대신
명령 문자열을 클립보드에 **쓰고**(읽지 않음), 코드에 하드코딩된 공식 문서 URL 2개만
브라우저로 넘깁니다. 실행은 사용자가 자기 터미널에서 합니다.

### 업데이트 확인 — G1 이 완전한 "네트워크 0" 이 아닌 이유

초기 버전에는 네트워크 코드가 **아예** 없었습니다. 그게 가장 강한 보증이었지만, 배포한
dmg 를 받은 사람이 새 버전을 알 방법이 없다는 문제가 남았습니다. 그래서 네트워크를
"없음" 에서 **"검사 가능한 최소 1건"** 으로 바꿨습니다. 이 절충은 숨길 대상이 아니라
문서화 대상입니다.

실제로 하는 일 (`UpdateChecker.swift`):

- **요청 1건, 하루 1회.** `GET https://api.github.com/repos/osh0678/toki/releases/latest`
- **인증 없음.** 토큰·API 키를 붙이지 않습니다.
- **업로드 본문 없음.** 쿼리 파라미터도, 식별자도, 버전 번호도, 텔레메트리도 보내지
  않습니다. 버전 비교는 응답을 받은 뒤 **로컬에서** 합니다.
- **응답에서 읽는 것은 `tag_name` 문자열 하나.** 나머지 필드는 파싱 결과에서 버립니다.
- **끌 수 있습니다.** 설정 → 공통 → 업데이트 확인. 끄면 요청이 0건이 되어 초기 버전과
  동일한 상태가 됩니다.
- 실패하면 조용히 넘어갑니다. 오프라인·타임아웃·형식 변경 모두 `nil` 로 처리합니다.

GitHub 가 알게 되는 것은 임의의 웹 요청이 드러내는 것과 같습니다 — 어떤 IP 가 공개
엔드포인트를 한 번 호출했다는 사실. 어느 계정이 얼마나 썼는지, 어떤 도구를 쓰는지는
전송되지 않습니다.

### Codex 실시간 조회 — 두 번째 서브프로세스를 허용한 이유

Codex 사용률은 원래 `~/.codex/sessions` 로그를 파싱해서 얻었습니다. 그런데 그 로그는
**Codex CLI 가 요청을 보낼 때만** 갱신되므로, 데스크톱 앱·웹·다른 기기에서 쓰면 로컬
숫자가 멈춥니다. 실제로 28시간 묵은 "72% 남음" 을 보여주는 동안 계정 잔량은 47% 였습니다.
틀린 숫자를 자신 있게 보여주는 상태였습니다.

`codex app-server` 의 JSON-RPC `account/rateLimits/read` 로 바꿨습니다. Codex TUI 의
`/usage` 가 쓰는 것과 같은 경로입니다.

**Claude 위임과 같은 논리입니다.** 자격증명은 Toki 가 아니라 `codex app-server` 프로세스가
자기 안에서 다루고, Toki 는 응답 JSON 의 **숫자와 플랜 문자열만** 읽습니다.
`~/.codex/auth.json` 은 열지 않습니다. 오히려 Claude 쪽보다 나은 점이 하나 있습니다 —
`claude -p "/usage"` 는 요청 1건을 소모하지만 이건 **순수 조회라 토큰·할당량을 쓰지
않습니다.**

**대신 위험은 더 큽니다.** 같은 소켓에 `command/exec`, `fs/writeFile`,
`mcpServer/tool/call`, `thread/shellCommand` 가 함께 노출돼 있습니다. 즉 이 프로세스는
마음만 먹으면 임의 명령 실행 통로가 됩니다. 그래서 Toki 가 보내는 메서드를 딱 둘
(`initialize`, `account/rateLimits/read`)로 고정하고, **그 외 변경성 메서드가 소스 어디에든
나타나면 빌드가 실패하도록** 검사를 추가했습니다. 편의를 위해 메서드 하나를 더 부르고
싶어지는 지점이 정확히 여기입니다.

조회에 실패하면(Codex 미설치·미로그인·타임아웃) 기존 로그 파싱으로 폴백하고, 그 값이
오래되면 나이를 함께 표시합니다.

### 자동 설치 — 원격 파일이 실행 코드가 되는 지점

이 문서의 이전 판은 자동 설치를 **금지**했습니다. 근거는 "ad-hoc 서명이라 교체될 번들의
서명을 검증할 수단이 없다" 였고, 그 근거가 사라졌기 때문에 결론도 바뀌었습니다. 바뀐 것과
바뀌지 않은 것을 나눠 적습니다.

**신뢰 기점은 Apple 이 아니라 릴리스 키입니다.** Apple 코드 서명은 "공증을 거쳤는가"에
답하고, ad-hoc 빌드는 그 질문에 아예 답할 수 없습니다. 대신 Ed25519 서명이 다른 질문에
답합니다 — **"이 아카이브를 릴리스 개인키 보유자가 만들었는가"**. 자기 교체에는 이 질문의
답이면 충분합니다. 공개키(32바이트)는 `UpdateInstaller.swift` 에 **컴파일되어** 들어가므로,
조작된 `config.json` 도 악의적인 GitHub 응답도 다른 키로 바꿔치기할 수 없습니다. 개인키는
`~/.toki/release.key` (0600) 에 있고 이 저장소에 들어오지 않습니다.

순서가 보증의 전부입니다:

1. 아카이브를 **디스크로** 스트리밍해 받습니다 (64MB 상한). 메모리에 버퍼링하지 않습니다.
2. 서명(정확히 64바이트)을 받습니다.
3. **검증에 실패하면 여기서 끝납니다.** 압축도 풀지 않고, 아무것도 교체하지 않습니다.
4. 검증을 통과해야 같은 볼륨의 임시 디렉터리에 풀고, 번들 구조와 **버전**을 확인합니다.
   유효하게 서명된 **과거** 릴리스를 되먹이는 공격을 막으려면 서명만으로는 부족하므로,
   푼 번들이 확인 단계에서 announce 된 버전과 같고 실행 중 버전보다 새로운지 봅니다.
5. 그제서야 `replaceItemAt` 으로 교체합니다 — 같은 볼륨이라 rename 이고, 앱이 어느 쪽에도
   없는 순간이 파일시스템이 만들 수 있는 만큼 짧습니다.

**서브프로세스를 쓰지 않습니다.** dmg 를 풀려면 `hdiutil`, zip 이면 `unzip`·`ditto` —
전부 외부 프로세스입니다. 업데이터가 하나라도 띄우면 G3(외부 프로세스는 한 파일에서만)이
깨집니다. 그래서 배포 형식이 Apple Archive(`.aar`) 입니다. macOS 가 **프레임워크로** 풀 수
있는 유일한 컨테이너이고, 코드 서명과 xattr 을 그대로 보존합니다.

**기본값은 꺼짐이고, 재시작은 사용자가 합니다.** 설정 → 공통 → 자동 설치를 켠 적이 없으면
아무 일도 일어나지 않고, 예전처럼 브라우저로 dmg 를 받습니다. 켠 경우에도 교체까지만 하고
프로세스를 죽이지 않습니다 — 실행 중인 이 프로세스는 어차피 옛 코드를 계속 돌리므로,
사용자를 대신해 종료해봐야 다음 실행에서 공짜로 얻을 것을 방해하며 얻을 뿐입니다.
`UpdateCard` 가 "지금 재시작" 버튼을 대신 내놓습니다.

**그래도 이것이 가장 위험한 코드입니다.** 아래 § 잔여 리스크에 릴리스 키 유출과 롤백
부재를 그대로 적어 두었습니다.

**그래도 G1 이 핵심입니다.** 유출 통로가 딱 한 곳으로 좁혀지고 그 한 곳이 "업로드 본문
없는 공개 GET" 이면, 설령 Toki 가 민감한 데이터를 읽더라도 실어 보낼 자리가 없습니다.
검사 스크립트가 `URLSession` 을 그 한 파일 밖으로 나가지 못하게 고정하는 이유입니다.

## 4. 검증 방법 — 자동화

`./security-check.sh` 가 매 빌드 후 위 보증을 기계적으로 검증합니다. 소스 검사 12건 +
컴파일된 바이너리 검사 6건, 하나라도 깨지면 종료 코드 1. `package-dmg.sh` 는 이 게이트를
통과하지 못하면 dmg 를 만들지 않습니다.

```
── 소스 검사 ──────────────────────────────────────
  ✓ 저수준 네트워크 API 미사용 (Network/CFStream/소켓)
  ✓ URLSession 은 UpdateChecker.swift 에서만
  ✓ Keychain/Security API 미사용
  ✓ 자격증명 파일 경로 미참조
  ✓ 셸 경유 실행 없음
  ✓ 외부 프로세스는 ClaudeOfficialUsage.swift 에서만
  ✓ 파일 쓰기는 SettingsWriter.swift 에서만
  ✓ URL 열기·클립보드는 InstallHelpCard/UpdateCard 에서만
  ✓ 클립보드 읽기 없음
  ✓ 전역 키보드 감시 없음

── 바이너리 검사 ──────────────────────────────────
  ✓ Network.framework / Security 프레임워크 미링크
  ✓ Keychain 심볼 미임포트
  ✓ 바이너리에 자격증명 문자열 없음
  ✓ 네트워크 entitlement 없음
  ✓ hardened runtime 적용
  ✓ 코드 서명 검증 통과

전체 통과 — 자격증명 접근·네트워크 경로 없음
```

검사의 성격이 바뀐 지점을 분명히 해둡니다. 업데이트 확인이 들어오면서 "네트워크 API 를
전혀 쓰지 않는다" 는 검사는 성립하지 않게 되었고, 대신 **`URLSession` 이 한 파일 밖으로
새지 않는지**를 검사합니다(`confine_source`). 다른 파일에 `URLSession` 을 한 줄이라도
추가하면 게이트가 실패합니다. 저수준 소켓 계열과 `Network.framework` 는 여전히 전면
금지이므로, 검사를 우회해서 조용히 통신을 추가할 경로도 남아 있지 않습니다.

소스 검사만으로는 부족하기 때문에 **컴파일 결과물도 함께** 검사합니다.

## 5. 잔여 리스크 (숨기지 않고 명시)

| 리스크 | 평가 | 대응 |
|---|---|---|
| **App Sandbox 미적용** | 샌드박스를 켜면 `~/.claude`·`~/.codex` 를 읽을 수 없어 위젯이 성립하지 않습니다. 대신 네트워크·Keychain 경로를 제거해 "읽을 수는 있지만 내보낼 수 없는" 상태로 만들었습니다. | G1–G3 로 완화 |
| **사용자 권한으로 홈 디렉터리 읽기 가능** | Toki 는 사용자가 이미 읽을 수 있는 파일만 읽습니다. 권한 상승은 없습니다. | 설계상 수용 |
| **ad-hoc 서명 (공증 없음)** | 로컬 빌드용이므로 Apple 공증을 받지 않습니다. 배포하려면 Developer ID 서명 + notarization 이 필요합니다. | 로컬 사용 전제 |
| **바깥 클릭 감지에 전역 마우스 모니터 사용** | 클릭의 *발생 사실*만 쓰고 값·좌표는 읽지 않으며, 패널이 떠 있는 동안에만 등록됩니다. 키보드 모니터는 전역·로컬 모두 없습니다. 접근성·입력 모니터링 권한은 필요하지 않습니다. | § 이벤트 모니터 로 범위 고정 + 검사로 전역 키보드 차단 |
| **업데이트 확인이 네트워크를 1건 씀** | "네트워크 코드 없음" 이라는 초기 보증을 포기한 지점입니다. 인증·업로드 본문·식별자가 없는 공개 GET 1건/일이며, 응답에서 `tag_name` 한 개만 읽고, 설정에서 끄면 0건이 됩니다. 다만 요청 자체가 IP 를 드러내고, 앞으로 이 파일에 코드가 늘면 유출 통로가 될 수 있습니다. | § 업데이트 확인 참조. `URLSession` 을 `UpdateChecker.swift` 로 고정하는 검사로 확산 차단 + 사용자 opt-out |
| **GitHub API 응답을 신뢰함** | 응답에서 읽는 것은 버전 문자열 하나입니다. 자동 설치가 꺼져 있으면 그것으로 하는 일은 화면 표시뿐이라 최대 피해는 "잘못된 버전 번호". 켜져 있어도 다운로드 URL 은 응답이 아니라 **코드에 하드코딩**되어 있고, 받은 아카이브는 컴파일된 공개키로 검증해야만 설치되므로, 조작된 응답이 할 수 있는 최대치는 "설치가 안 되게 만들기" 입니다. | URL 하드코딩 + 서명 검증 |
| **릴리스 개인키 유출** | 이 설계에서 가장 큰 단일 실패 지점입니다. `~/.toki/release.key` 를 얻은 사람은 자동 설치를 켠 모든 사용자에게 임의 코드를 배포할 수 있습니다. 앱이 검증하는 것은 "릴리스 키 보유자가 만들었는가" 이지 "내용이 안전한가" 가 아닙니다. | 키는 저장소 밖 0600 파일, 공개키는 바이너리에 고정. **완화일 뿐 제거가 아님** — 자동 설치를 끄면 이 리스크는 0 |
| **롤백 없음** | 새 버전이 깨져 있어도 앱이 되돌리지 못합니다. 교체 전 번들은 `replaceItemAt` 이 정리하므로 남지 않습니다. | 이전 버전 dmg 를 직접 받아 덮어써야 함. 자동 설치 기본값이 꺼짐인 이유 중 하나 |
| **자동 설치가 파일시스템 쓰기를 넓힘** | G4 가 "설정 파일 하나" 에서 "설정 파일 + 자기 번들" 로 넓어졌습니다. 앱이 설치된 디렉터리에 쓸 권한이 없으면 시도 자체를 하지 않습니다. | 대상은 `Bundle.main.bundleURL` 하나로 고정, `.app` 확장자가 아니면 거부(개발 중 실행을 절대 교체하지 않음), 검증 통과 후에만 실행 |
| **로그 포맷 변경 시 오탐** | 필드가 사라지면 해당 카드가 "기록 없음"으로 표시됩니다. 잘못된 숫자를 자신 있게 보여주는 것보다 안전한 실패 방향입니다. | 설계상 의도 |
| **툴체인/의존성 공급망** | 외부 SwiftPM 의존성이 **0개**입니다. Apple SDK 만 사용합니다. | 공격면 최소 |

## 6. OAuth 로 공식 수치를 가져오는 방안 — 검토 결과

"Claude 쪽도 공식 수치를 쓸 수 없나"에 대한 답입니다.

| 방안 | 가능성 | 보안 영향 |
|---|---|---|
| **A. 현재 방식** (로컬 로그 집계) | 동작함. 단 비율은 자체 보정 추정치 | 자격증명 0, 네트워크 0 |
| **B. 공개 API 키** (`sk-ant-…`) | **불가.** 공개 API 에 구독제 5시간/주간 창을 주는 엔드포인트가 없음 | — |
| **C. Admin API** (`sk-ant-admin…`) | **부적합.** `/v1/organizations/usage_report/messages` 는 조직의 **API** 토큰 소비량이라 구독 세션 한도와 다른 지표 | 조직 전체 권한 키를 위젯이 보유해야 함 — 리스크 상승 |
| **D. Keychain OAuth 토큰 + 내부 엔드포인트** | 정확한 수치 획득 가능. Claude Code `/usage` 가 쓰는 **비공개** 경로 | **G1·G2 가 모두 깨짐.** 앱이 살아있는 액세스 토큰을 보유하고 네트워크를 열게 됨 |
| **E. 로컬 캐시 재활용** | **불가.** `~/.claude.json` 의 `clientDataCacheSlots` 를 확인했으나 GrowthBook 기능 플래그(`tengu_*`, `cedar_*`)만 있고 한도/사용량 필드가 없음 | — |

| **F. `claude` CLI 위임** (채택) | 동작함. 공식 수치를 그대로 얻음 | **G1·G2 유지.** 자격증명은 CLI 프로세스가 관리하고 Toki 는 stdout 만 읽음. 비용은 서브프로세스 1건(G3 로 좁힘) |

현재 구현은 **F** 입니다. D 와 같은 정확도를 얻으면서 네트워크·Keychain 보증을 지킵니다.
남는 대가는 두 가지이고 둘 다 문서화했습니다: 호출 1회가 요청 1건을 소모한다는 점(그래서
기본 10분 주기), 그리고 `/usage` 출력이 사람이 읽는 형식이라 Claude Code 업데이트로 문구가
바뀌면 파싱이 실패할 수 있다는 점(실패 시 조용히 추정치로 되돌아가고 카드에 사유 표시).

정확도가 더 필요하고 호출도 줄이고 싶다면, `/usage` 를 한 번 확인해 실제 한도를
`config.json` 에 넣고 `useClaudeCLI` 를 끄는 조합(A)도 그대로 가능합니다.

## 7. 변경 시 체크리스트

이 저장소에 손을 댈 때:

1. Keychain·서브프로세스 API 를 추가하지 마세요. 네트워크는 `UpdateChecker.swift` 와
   `UpdateInstaller.swift` 밖에서 쓰지 마세요 — `URLSession` 을 다른 파일에 넣으면
   `security-check.sh` 가 실패하며, 실패는 곧 위 보증이 깨졌다는 뜻입니다.
2. 앱이 실행 파일·앱 번들·스크립트를 쓰거나 받는 곳은 `UpdateInstaller.swift` **하나로
   유지**하세요. 그 파일 안에서도 다음 세 가지는 협상 대상이 아닙니다:
   **(a)** 서명 검증이 압축 해제·교체보다 **먼저** 일어날 것,
   **(b)** 공개키는 코드에 컴파일되어 있을 것 — 설정·응답·환경변수에서 읽지 말 것,
   **(c)** 서브프로세스를 띄우지 말 것 (그래서 dmg·zip 이 아니라 Apple Archive 입니다).
   이 중 하나라도 무너지면 원격 파일이 그냥 실행 코드가 되고, 나머지 보증이 전부
   무의미해집니다. 편의를 위해 순서를 바꾸고 싶어지는 지점이 정확히 여기입니다.
3. 새 파일을 읽으려면 경로를 코드에 하드코딩하고, 설정에서 경로를 받지 마세요.
4. 로그에서 새 필드를 꺼낼 때는 숫자/열거형만 꺼내세요. 자유 텍스트를 UI 로 올리면
   프롬프트 내용이 화면에 노출될 수 있습니다.
5. 빌드 후 항상 `./security-check.sh` 를 돌리세요.
