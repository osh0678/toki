# 변경 이력

이 프로젝트는 [Semantic Versioning](https://semver.org/lang/ko/) 을 따릅니다.

## 1.0.0

첫 공개 릴리스.

### 기능

- **메뉴바 위젯** — 토끼 아이콘과 남은 여유 퍼센트를 상시 표시. 좌클릭으로 Liquid Glass
  패널을 열고, 우클릭으로 새로고침·종료 메뉴를 띄웁니다.
- **Claude Code 공식 사용량** — `claude -p "/usage"` 를 통해 5시간·주간 창의 실제 잔량과
  초기화 시각을 가져옵니다. 인증은 CLI 가 처리하므로 Toki 는 Keychain 도 네트워크도
  사용하지 않습니다.
- **Codex CLI 공식 사용량** — 세션 로그의 `rate_limits` 스냅샷을 읽어 추가 호출 없이
  서버가 보고한 사용률을 표시합니다.
- **토큰·비용 집계** — Claude 세션 로그를 직접 집계해 오늘 사용한 토큰과 API 정가 환산액을
  보여줍니다(구독 청구액이 아닌 참고치).
- **잔량 계기** — 소비된 구간을 채우지 않고 비워, 패널 뒤 배경이 그래프를 통과합니다.
  25% 아래에서 주황, 10% 아래에서 빨강으로 전환됩니다.
- **설정 화면** — Claude Code / Codex CLI / 공통 섹션으로 분리. 연동 상태와 실패 사유를
  표시하고, 변경은 즉시 적용되며 저장 시 `~/.config/toki/config.json` 에 기록됩니다.
  배경 불투명도와 메뉴바 퍼센트 표시도 여기서 조정합니다. 변경은 자동 저장됩니다.
- **공식 수치를 못 가져올 때** — 로컬 로그 기반 추정치로 자동 전환하고 `추정` 으로
  표시합니다.

### 보안

- 네트워크 코드 없음(`URLSession`/`CFNetwork` 미링크), Keychain 미접근
  (`Security.framework` 미링크).
- 외부 프로세스 실행은 `ClaudeOfficialUsage.swift` 한 곳으로 한정(절대경로 허용목록,
  리터럴 인자, 셸 미사용).
- 디스크 쓰기는 설정 파일 하나뿐.
- 소스와 컴파일된 바이너리 양쪽을 검증하는 `security-check.sh` 게이트를 통과하지 못하면
  dmg 를 만들지 않습니다.

### 알려진 제약

- 공식 수치 호출 1회는 요청 1건을 소모합니다(기본 10분 주기).
- `/usage` 출력은 사람이 읽는 형식이라 정규식으로 파싱합니다. Claude Code 업데이트로 문구가
  바뀌면 공식 수치가 실패하고 추정치로 되돌아갑니다.
- ad-hoc 서명이라 Apple 공증이 없습니다. 첫 실행 시 Gatekeeper 허용이 필요합니다.
