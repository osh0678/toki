# 변경 이력

이 프로젝트는 [Semantic Versioning](https://semver.org/lang/ko/) 을 따릅니다.

## 1.1.5

### 변경

- **Codex 사용률을 실시간으로 조회합니다.** 이제 `codex app-server` 에 직접 물어봅니다 —
  Codex CLI 의 `/usage` 가 쓰는 것과 같은 경로입니다. **요청도 토큰도 소모하지 않습니다.**

  1.1.4 까지는 `~/.codex/sessions` 로그를 읽었는데, 그 로그는 Codex CLI 가 요청을 보낼
  때만 갱신됩니다. 그래서 데스크톱 앱·웹·다른 기기에서 쓰면 로컬 숫자가 멈췄습니다.
  실제로 28시간 묵은 "72% 남음" 을 보여주는 동안 계정 잔량은 47% 였습니다.

  조회에 실패하면(Codex 미설치·미로그인·타임아웃) 기존 로그 파싱으로 폴백하며, 그때는
  값의 나이를 함께 표시합니다.

### 보안

- Codex 조회를 위해 서브프로세스 허용 파일이 하나 늘었습니다(G3). 근거는
  [SECURITY.md](SECURITY.md) 의 § Codex 실시간 조회 에 적었습니다. 자격증명은 Toki 가
  아니라 `codex app-server` 가 자기 프로세스 안에서 다루고, Toki 는 응답의 숫자만 읽습니다.
- 같은 소켓에 명령 실행·파일 쓰기 메서드도 노출돼 있어, Toki 가 **조회 메서드만** 쓴다는
  것을 강제하는 검사를 추가했습니다. 다른 메서드가 소스에 나타나면 빌드가 실패합니다.

## 1.1.4

### 수정

- **Codex 사용률이 실제와 다르게 보이던 문제.** 계산은 맞았고, 값이 오래된 것이었습니다.
  Codex 는 자기가 요청을 보낼 때만 사용률을 로그에 남기므로, Codex 를 쓰지 않는 동안
  숫자가 그 시점에 멈춰 있습니다. 그동안 Toki 는 이를 현재값처럼 보여줬습니다. 이제
  스냅샷 시각을 함께 표시하고, 6시간이 넘으면 갱신 방법을 안내합니다.

  > Codex 서버 보고값 · 1일 전 기준 — Codex 를 다시 쓰면 갱신됩니다

  Toki 가 직접 최신값을 가져오려면 사용량을 재려고 요청을 소모하거나 `~/.codex/auth.json`
  의 토큰을 읽어야 합니다. 후자는 [SECURITY.md](SECURITY.md) 의 G1·G2 를 동시에 깨므로
  하지 않습니다.
- **설정 화면 아래쪽이 잘리던 문제.** 창 높이가 화면 크기로 제한되는데 스크롤이 없어서,
  내용이 화면보다 길면 아래쪽에 닿을 방법이 없었습니다. 스크롤을 추가했습니다.
- **업데이트 설치 후 "지금 재시작" 이 동작하지 않을 수 있던 문제.** 번들 교체는 디렉터리를
  맞바꾸는 방식이라, 실행 중이던 번들이 임시 디렉터리로 밀려납니다. 재시작 경로를 그때
  다시 계산하면 이미 사라진 옛 번들을 가리킬 수 있어, 교체 시점에 확정된 경로를 쓰도록
  했습니다.

## 1.1.3

자동 설치 경로를 실제 릴리스로 처음 끝까지 검증하기 위한 릴리스입니다. 코드 변경은
없습니다. 1.1.2 를 쓰고 계시면 굳이 받지 않아도 되며, 자동 설치를 켜두셨다면 이 버전이
스스로 교체되는지 확인하는 용도입니다.

## 1.1.2

### 추가

- **경고 표시 기준 설정.** 남은 여유가 몇 % 아래로 떨어질 때 막대와 메뉴바 아이콘이
  노란색으로 바뀔지 고를 수 있습니다 (5~50%, 기본 25%). 이전에는 25% 고정이었습니다.

### 변경

- 설정 화면 맨 아래 안내 문구를 없앴습니다. 컨트롤이 이미 설명하고 있는 내용이었습니다.

## 1.1.1

### 추가

- **설정에 현재 버전 표시.** 설정 → 공통 맨 위에 실행 중인 버전과 함께 `최신` /
  `새 버전 X` / `확인 안 함` 상태를 같이 보여줍니다.

### 수정

- **"Toki 가 파일에 접근하려고 합니다" 알림.** Toki 는 `~/.claude`·`~/.codex`·자기 설정만
  읽고 셋 다 접근 권한이 필요 없는 위치입니다. 권한을 요청한 것은 Toki 가 아니라 Toki 가
  띄운 `claude` CLI 였고, 자식 프로세스의 요청은 부모 앱 이름으로 표시됩니다. 작업
  디렉터리를 지정하지 않아 CLI 가 파일시스템 루트(`/`)를 프로젝트 디렉터리로 열고 있던
  것이 원인이라, 임시 디렉터리로 고정했습니다.

## 1.1.0

### ⚠️ 이번 한 번만 수동 설치입니다

자동 설치 기능이 **이 버전부터** 들어갑니다. 1.0.0 에는 그 코드가 없으므로, 1.0.0 을 쓰고
계시면 아래 dmg 를 직접 받아 교체해 주세요. 1.1.0 부터는 설정에서 켜두면 다음 버전을
알아서 받아 교체합니다.

### 추가

- **자동 설치 (기본값 꺼짐)** — 설정 → 공통 → 자동 설치. 켜면 새 버전을 백그라운드로 받아
  **Ed25519 서명을 검증한 뒤에만** 앱을 교체합니다. 검증에 실패하면 압축조차 풀지 않습니다.
  교체 후에도 앱을 강제로 종료하지 않고, "지금 재시작" 버튼으로 사용자가 시점을 정합니다.
  릴리스 공개키는 바이너리에 컴파일되어 있어 설정 파일이나 서버 응답으로 바꿔치기할 수
  없습니다. 자세한 내용은 [SECURITY.md](SECURITY.md) 의 § 자동 설치 참조.

### 수정

- 패널을 처음 클릭할 때 색이 변하던 문제. 패널이 key 상태가 아니어서 SwiftUI 가 비활성
  외형으로 그린 뒤 첫 클릭에 활성 외형으로 전환하던 것이 원인이었습니다.
- 설정 아이콘을 누르면 앱이 종료되던 크래시. SwiftUI 의 크기 변경이 Auto Layout 을 거쳐
  표시 사이클 도중 창 크기를 바꾸면서 재진입이 발생했습니다.
- README 최하단 문구가 겹쳐 보이던 문제.

### 보안 게이트

- 파일 쓰기 검사를 `replaceItemAt`·`moveItem`·`removeItem` 등 디렉터리 이동 API 까지
  포함하도록 확대. 번들 교체는 바이트를 쓰지 않아 기존 패턴을 그냥 통과했습니다.
- 서명 검증 코드의 존재 자체를 요구하는 검사 추가 — 보호 장치가 조용히 삭제되는 것을
  막습니다.

## 1.0.0

첫 공개 릴리스.

### 기능

- **메뉴바 위젯** — 토끼 아이콘과 남은 여유 퍼센트를 상시 표시. 좌클릭으로 Liquid Glass
  패널을 열고, 우클릭으로 새로고침·종료 메뉴를 띄웁니다.
- **Claude Code 공식 사용량** — `claude -p "/usage"` 를 통해 5시간·주간 창의 실제 잔량과
  초기화 시각을 가져옵니다. 인증은 CLI 가 처리하므로 Toki 는 Keychain 도 네트워크도
  사용하지 않습니다.
- **Codex CLI 공식 사용량** — 세션 로그의 `rate_limits` 스냅샷을 읽어 추가 호출 없이
  서버가 보고한 사용률을 표시합니다.
- **토큰·비용 집계** — Claude 세션 로그를 직접 집계해 오늘 사용한 토큰과 API 정가 환산액을
  보여줍니다(구독 청구액이 아닌 참고치).
- **잔량 계기** — 소비된 구간을 채우지 않고 비워, 패널 뒤 배경이 그래프를 통과합니다.
  25% 아래에서 주황, 10% 아래에서 빨강으로 전환됩니다.
- **설정 화면** — Claude Code / Codex CLI / 공통 섹션으로 분리. 연동 상태와 실패 사유를
  표시하고, 변경은 즉시 적용되며 저장 시 `~/.config/toki/config.json` 에 기록됩니다.
  배경 불투명도와 메뉴바 퍼센트 표시도 여기서 조정합니다. 변경은 자동 저장됩니다.
- **공식 수치를 못 가져올 때** — 로컬 로그 기반 추정치로 자동 전환하고 `추정` 으로
  표시합니다.

### 보안

- 네트워크 코드 없음(`URLSession`/`CFNetwork` 미링크), Keychain 미접근
  (`Security.framework` 미링크).
- 외부 프로세스 실행은 `ClaudeOfficialUsage.swift` 한 곳으로 한정(절대경로 허용목록,
  리터럴 인자, 셸 미사용).
- 디스크 쓰기는 설정 파일 하나뿐.
- 소스와 컴파일된 바이너리 양쪽을 검증하는 `security-check.sh` 게이트를 통과하지 못하면
  dmg 를 만들지 않습니다.

### 알려진 제약

- 공식 수치 호출 1회는 요청 1건을 소모합니다(기본 10분 주기).
- `/usage` 출력은 사람이 읽는 형식이라 정규식으로 파싱합니다. Claude Code 업데이트로 문구가
  바뀌면 공식 수치가 실패하고 추정치로 되돌아갑니다.
- ad-hoc 서명이라 Apple 공증이 없습니다. 첫 실행 시 Gatekeeper 허용이 필요합니다.
